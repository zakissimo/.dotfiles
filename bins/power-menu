#!/usr/bin/env bash

chosen=$(printf "  Power Off\n  Restart\n  Lock" | wofi -dmenu -p 'Power menu:')

case "$chosen" in
"  Power Off") poweroff ;;
"  Restart") reboot ;;
"  Lock") slock ;;
*) exit 1 ;;
esac
