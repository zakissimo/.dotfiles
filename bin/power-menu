#!/usr/bin/env bash

chosen=$(printf "  Power Off\n󰒲  Suspend\n  Restart\n  Lock" | rofi -dmenu)

case "$chosen" in
"  Power Off") poweroff ;;
"󰒲  Suspend") systemctl suspend-then-hibernate ;;
"  Restart") reboot ;;
"  Lock") swaylock ;;
*) exit 1 ;;
esac
